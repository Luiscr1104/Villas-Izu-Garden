---
import gallery1 from '@/assets/gallery1.png';
import gallery2 from '@/assets/gallery2.png';
import gallery3 from '@/assets/gallery3.png';
import gallery4 from '@/assets/gallery4.png';
import gallery5 from '@/assets/gallery5.png';
---

<section id="gallery" class="py-20 bg-[#796446]">
  <div class="container mx-auto px-4">
    
    <!-- Título -->
    <div class="text-center mb-16" data-aos="fade-up">
      <h2 class="text-3xl font-serif font-bold text-[#FFF0DA] mb-4">Galería</h2>
      <p class="max-w-2xl mx-auto text-lg text-[#FFF0DA]">
        Explora nuestra galería y déjate inspirar por la belleza de Villas Izu Garden.
      </p>
    </div>

    <!-- Galería de imágenes -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      {[
        { src: gallery1, alt: 'Exterior', text: 'Exteriores de Villa Sakura' },
        { src: gallery2, alt: 'Piscina', text: 'Piscina Infinita' },
        { src: gallery3, alt: 'Interior', text: 'Sala de Estar' },
        { src: gallery4, alt: 'Habitación', text: 'Habitación Principal' },
        { src: gallery5, alt: 'Restaurante', text: 'Restaurante' },
        { src: gallery1, alt: 'Spa', text: 'Spa y Bienestar' },
      ].map((img, i) => (
        <div
          class="relative group overflow-hidden rounded-lg"
          data-aos="zoom-in"
          data-aos-delay={`${i * 100}`}
        >
          <img 
            src={img.src.src}
            alt={img.alt}
            class="w-full h-80 object-cover transition-transform duration-500 group-hover:scale-110"
          />
          <div class="absolute inset-0 bg-black/50 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center">
            <p class="text-[#FFF0DA] font-medium text-lg">{img.text}</p>
          </div>
        </div>
      ))}
    </div>

    <!-- CTA -->
    <div class="text-center mt-12" data-aos="fade-up" data-aos-delay="600">
      <a 
        href="#"
        class="inline-block border-2 border-[#FFF0DA] text-[#FFF0DA] px-6 py-3 rounded-lg hover:bg-[#FFF0DA]/10 transition-colors"
      >
        Ver galería completa
      </a>
    </div>
  </div>
</section>
